<%= form_for(@picture) do |f| %>
  <% if @picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% @picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"タイトル" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.file_field :photo %>
  </div>
  <div class="field">
    <%= f.label :"説明" %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :"必要としている人物" %><br>
    <%= f.text_field :required_user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
